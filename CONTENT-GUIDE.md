# راهنمای ساختار محتوا (برای ایجنت / بروزرسانی خودکار)

این سایت طوری طراحی شده که یک ایجنت هوش‌مصنوعی (مثل Claude) بتونه بدون دست‌زدن به HTML یا CSS، محتوا اضافه/ویرایش/حذف کنه. کافیه این راهنما رو به ایجنت بدی.

## ۱) افزودن پروژه‌ی جدید به بخش «نمونه پروژه‌ها»
فقط فایل `data/projects.json` رو ویرایش کن:
1. عکس پروژه رو در `images/projects/` قرار بده (نام فایل بدون فاصله، انگلیسی).
2. یک آبجکت جدید مطابق نمونه‌های موجود به آرایه‌ی `projects` اضافه کن (فیلدها: id, image, title_fa, title_en, role_fa, role_en).
3. جدیدترین پروژه رو ابتدای آرایه بذار.
هیچ فایل دیگه‌ای نیاز به تغییر نداره — صفحه‌ی اصلی (فارسی و انگلیسی) خودکار آپدیت می‌شه.

## ۲) افزودن مقاله‌ی جدید به وبلاگ
1. یک فایل html جدید برای متن کامل مقاله در `blog/` بساز (از `blog/lean-construction-chist.html` به‌عنوان قالب کپی کن) و نسخه‌ی انگلیسی‌ش رو هم در `en/blog/` بساز.
2. یک آبجکت جدید به ابتدای آرایه‌ی `posts` در `data/posts.json` اضافه کن؛ `status` را `"published"` بگذار و در `link_fa` / `link_en` مسیر فایل‌های تازه‌ساخته‌شده (نسبت به ریشه‌ی سایت، مثلاً `blog/my-new-post.html`) را وارد کن.
3. برای `icon` یکی از این مقادیر رو انتخاب کن: `lean`, `pm`, `km`, `risk`, `training`, `expert` (یا خالی بگذار).
این تغییر هم در صفحه‌ی اصلی (۳ مقاله‌ی اخیر) و هم در صفحه‌ی وبلاگ (همه‌ی مقالات) خودکار اعمال می‌شه — هم فارسی هم انگلیسی.

## ۳) ویرایش متن‌های ثابت (خدمات، رزومه، درباره من)
این بخش‌ها مستقیم در فایل‌های html نوشته شدن (چون به‌ندرت تغییر می‌کنن). برای ویرایش، فایل مربوطه رو مستقیم باز کن:
- `index.html` / `en/index.html` — صفحه‌ی اصلی
- `services.html` / `en/services.html` — خدمات
- `resume.html` / `en/resume.html` — سوابق
- `contact.html` / `en/contact.html` — تماس

⚠️ همیشه نسخه‌ی فارسی و انگلیسی رو با هم آپدیت کن تا سایت هماهنگ بمونه.

## ۴) قوانین مهم برای ایجنت
- هرگز کلاس‌های CSS موجود رو تغییر نده یا حذف نکن؛ فقط محتوای متنی/تصویری رو عوض کن.
- بعد از هر تغییر در فایل‌های json، ساختار json رو با یک اعتبارسنج (مثلاً `python3 -m json.tool data/posts.json`) چک کن تا خراب نشده باشه.
- عکس‌های جدید رو قبل از آپلود کوچک/بهینه کن (عرض حداکثر ۱۰۰۰ پیکسل، فرمت jpg، کیفیت حدود ۸۰).
- بعد از هر تغییر، `sitemap.xml` رو در صورت افزودن صفحه‌ی جدید (مثل مقاله‌ی وبلاگ) به‌روز کن تا سئو درست بمونه.
